import { makeStyles } from "@material-ui/core/styles";

export default makeStyles ((theme) => ({
    content: {
        flexGrow: 1,
        paddingTop: '5rem',
        alignItems: 'center'
    },
    homeText: {
      color: '#bbd884',
      lineHeight: '140%'
    },
    homeImg: {
      width: '360px',
      height: '380px',
    }
}))